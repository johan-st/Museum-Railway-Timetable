{
  "name": "museum-railway-timetable",
  "private": true,
  "description": "WordPress plugin – optional Node tests for shared JS utils",
  "scripts": {
    "test:js": "node --test tests/js/*.mjs"
  }
}
